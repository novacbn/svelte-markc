const {code_braces_handler, code_meta_handlers} = require("./code");
const {tag_sensitivity_handlers} = require("./html");

module.exports = {
    code_braces_handler,
    code_meta_handlers,
    tag_sensitivity_handlers
};
